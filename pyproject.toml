[project]
name = "ha-cbus-cgate"
version = "0.4.6"
requires-python = ">=3.13"

[tool.pytest.ini_options]
pythonpath = ["custom_components/cbus_cgate"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]
